Flask==3.0.0
Flask-CORS==4.0.0
PyPDF2==3.0.1
Werkzeug==3.0.1

# PDF OCR for image-heavy papers (Physics, etc.)
pdf2image>=1.16.0  # Convert PDF pages to images
pytesseract>=0.3.10  # OCR text extraction from images
Pillow>=10.0.0  # Image processing

# AI/LLM integrations (optional but recommended)
groq>=0.4.0  # Groq API - RECOMMENDED (FREE, FAST, no safety filters)
google-generativeai>=0.3.0  # Google Gemini API (free but strict safety filters)
openai>=1.3.0  # OpenAI API (optional)
python-dotenv>=1.0.0  # For loading .env files with API keys

# Streamlit UI (alternative to Flask)
streamlit>=1.28.0
plotly>=5.17.0  # For interactive charts in Streamlit
